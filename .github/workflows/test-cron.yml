name: Cron Test

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Time
        run: |
          echo "Cron Triggered"
          date
